FROM nvidia/cuda:12.1.1-cudnn8-runtime-ubuntu22.04

ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && apt-get install -y --no-install-recommends \
    python3 python3-pip python3-venv git ffmpeg \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

COPY requirements_video.txt /app/requirements.txt
RUN python3 -m pip install --upgrade pip && pip3 install --no-cache-dir -r /app/requirements.txt

COPY video_handler_template.py /app/handler.py

CMD ["python3", "-u", "/app/handler.py"]

